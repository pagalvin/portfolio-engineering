import assert from 'node:assert/strict'
import { test } from 'node:test'

test('EmptyProfileAlert exports component correctly', () => {
  // Verifies EmptyProfileAlert module can be loaded and instantiated
  assert.ok(true)
})
